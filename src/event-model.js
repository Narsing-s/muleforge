function buildEventModel(config={}){const triggers=[...(config.triggers||[]),...(config.events||[])].map((e,i)=>({id:e.id||e.name||`event-${i+1}`,type:String(e.type||e.trigger||"http").toLowerCase(),source:e.source||null,destination:e.destination||null,topic:e.topic||null,queue:e.queue||null,retry:e.retry||{maxAttempts:3},deadLetterQueue:e.deadLetterQueue||e.dlq||null,idempotency:Boolean(e.idempotency),correlationId:e.correlationId!==false}));return {version:"1.0",triggers};}
function validateEventModel(model){const allowed=new Set(["http","scheduler","file","sftp","jms","ibm-mq","anypoint-mq","kafka"]);const errors=[];for(const e of model.triggers||[]){if(!allowed.has(e.type))errors.push(`Unsupported trigger type: ${e.type}`);if(["kafka","ibm-mq","anypoint-mq","jms"].includes(e.type)&&!e.queue&&!e.topic&&!e.source)errors.push(`Messaging trigger ${e.id} needs a source/topic/queue`);}return {valid:!errors.length,errors};}
module.exports={buildEventModel,validateEventModel};