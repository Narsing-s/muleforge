const fs=require("node:fs"),path=require("node:path"),YAML=require("yaml");
function walk(d,out=[]){if(!fs.existsSync(d))return out;for(const e of fs.readdirSync(d,{withFileTypes:true})){if([".git","target","node_modules"].includes(e.name))continue;const p=path.join(d,e.name);e.isDirectory()?walk(p,out):out.push(p);}return out;}
function attrs(tag){const a={};for(const m of String(tag).matchAll(/([A-Za-z_:][\w:.-]*)\s*=\s*"([^"]*)"/g))a[m[1]]=m[2];return a;}
function extract(xml,file){const out=[];const re=/<flow\b[^>]*name="([^"]+)"[\s\S]*?<\/flow>/gi;let m;while((m=re.exec(xml))){const body=m[0],lm=body.match(/<http:listener\b([^>]*)\/?>(?:<\/http:listener>)?/i);if(lm){const a=attrs(lm[1]);out.push({name:m[1],method:(a.method||"GET").toUpperCase(),path:a.path||"/",connector:"http",source:file});}}
for(const m2 of xml.matchAll(/<([\w-]+):([\w-]+)\b([^>]*)\/?>(?:<\/\1:\2>)?/g)){const ns=m2[1].toLowerCase(),op=m2[2].toLowerCase();if(["db","snowflake","sftp","kafka","jms","ibm-mq","anypoint-mq","sfdc"].includes(ns))out.push({name:ns+"-"+op,method:"EVENT",path:"/",connector:ns,action:op,source:file});}return out;}
function importProject(root="."){const base=path.resolve(root),files=walk(base),xml=files.filter(f=>f.endsWith(".xml")),raml=files.filter(f=>f.endsWith(".raml")),dw=files.filter(f=>f.endsWith(".dwl")),munit=files.filter(f=>/munit/i.test(f)&&f.endsWith(".xml")),pom=files.find(f=>path.basename(f)==="pom.xml");const operations=[];for(const f of xml)operations.push(...extract(fs.readFileSync(f,"utf8"),path.relative(base,f).replace(/\\/g,"/")));const unique=[...new Map(operations.map(o=>[JSON.stringify([o.name,o.path,o.connector,o.action]),o])).values()];const configs=files.filter(f=>/application.*\.(yaml|yml|properties)$/.test(f)).map(f=>path.relative(base,f).replace(/\\/g,"/"));return {version:"1.1",project:{name:path.basename(base),artifactId:path.basename(base),version:"1.0.0"},api:{name:path.basename(base),version:"v1",basePath:"/api/v1"},operations:unique,inventory:{files:files.length,muleXml:xml.length,raml:raml.length,dataWeave:dw.length,munit:munit.length,pom:Boolean(pom),environmentConfigs:configs},migration:{reviewRequired:true,preserveSource:true,unmappedAssets:files.filter(f=>/\.(dwl|xml|raml|yaml|yml|properties)$/i.test(f)).map(f=>path.relative(base,f).replace(/\\/g,"/"))},ramlSources:raml.map(f=>path.relative(base,f).replace(/\\/g,"/")),dataWeaveSources:dw.map(f=>path.relative(base,f).replace(/\\/g,"/"))};}
function writeImportedModel(root="."){const model=importProject(root),target=path.join(path.resolve(root),"muleforge-import.yaml");fs.writeFileSync(target,YAML.stringify(model),"utf8");return {target,model};}
module.exports={importProject,writeImportedModel};