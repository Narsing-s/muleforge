<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:db="http://www.mulesoft.org/schema/mule/db"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd
        http://www.mulesoft.org/schema/mule/ee http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

    <configuration-properties file="application.yaml" />

    <http:listener-config name="HTTP_Listener_config">
        <http:listener-connection host="0.0.0.0" port="${http.port}" />
    </http:listener-config>

    <db:config name="Database_Config">
        <db:generic-connection
            url="${db.url}"
            driverClassName="net.snowflake.client.jdbc.SnowflakeDriver"
            user="${db.user}"
            password="${db.password}" />
    </db:config>

    <flow name="create-customer-flow">
        <http:listener config-ref="HTTP_Listener_config" path="/api/v1/customers" allowedMethods="POST">
            <http:response statusCode="#[vars.httpStatus default 201]" />
        </http:listener>

        <ee:transform>
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
if (isEmpty(payload.name default "") or isEmpty(payload.email default "") or isEmpty(payload.mobileNumber default ""))
    error({type: "VALIDATION", description: "name, email and mobileNumber are required"})
else if (!((payload.email as String) matches /.+@.+\..+/))
    error({type: "VALIDATION", description: "Invalid email"})
else payload]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <try>
            <set-variable variableName="request" value="#[payload]" />
            <db:select config-ref="Database_Config">
                <db:sql><![CDATA[SELECT CUSTOMER_ID FROM CUSTOMER WHERE EMAIL = :email LIMIT 1]]></db:sql>
                <db:input-parameters><![CDATA[#[{ email: vars.request.email }]]]></db:input-parameters>
            </db:select>

            <choice>
                <when expression="#[sizeOf(payload) &gt; 0]">
                    <set-variable variableName="httpStatus" value="409" />
                    <ee:transform>
                        <ee:message>
                            <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ status: "FAILED", code: "CUSTOMER_EXISTS", message: "Customer already exists" }]]></ee:set-payload>
                        </ee:message>
                    </ee:transform>
                </when>
                <otherwise>
                    <set-variable variableName="customerId" value="#[uuid()]" />
                    <db:insert config-ref="Database_Config">
                        <db:sql><![CDATA[INSERT INTO CUSTOMER (CUSTOMER_ID, NAME, EMAIL, MOBILE_NUMBER) VALUES (:customerId, :name, :email, :mobileNumber)]]></db:sql>
                        <db:input-parameters><![CDATA[#[{
                            customerId: vars.customerId,
                            name: vars.request.name,
                            email: vars.request.email,
                            mobileNumber: vars.request.mobileNumber
                        }]]]></db:input-parameters>
                    </db:insert>
                    <set-variable variableName="httpStatus" value="201" />
                    <ee:transform>
                        <ee:message>
                            <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ customerId: vars.customerId, status: "SUCCESS" }]]></ee:set-payload>
                        </ee:message>
                    </ee:transform>
                </otherwise>
            </choice>

            <error-handler>
                <on-error-continue type="ANY" logException="true">
                    <set-variable variableName="httpStatus" value="500" />
                    <ee:transform>
                        <ee:message>
                            <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ status: "FAILED", code: "CUSTOMER_CREATE_FAILED", message: "Customer could not be created" }]]></ee:set-payload>
                        </ee:message>
                    </ee:transform>
                </on-error-continue>
            </error-handler>
        </try>
    </flow>

    <flow name="get-customer-flow">
        <http:listener config-ref="HTTP_Listener_config" path="/api/v1/customers/{customerId}" allowedMethods="GET">
            <http:response statusCode="#[vars.httpStatus default 200]" />
        </http:listener>

        <try>
            <db:select config-ref="Database_Config">
                <db:sql><![CDATA[SELECT CUSTOMER_ID, NAME, EMAIL, MOBILE_NUMBER FROM CUSTOMER WHERE CUSTOMER_ID = :customerId LIMIT 1]]></db:sql>
                <db:input-parameters><![CDATA[#[{ customerId: attributes.uriParams.customerId }]]]></db:input-parameters>
            </db:select>

            <choice>
                <when expression="#[sizeOf(payload) == 0]">
                    <set-variable variableName="httpStatus" value="404" />
                    <ee:transform>
                        <ee:message>
                            <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ status: "FAILED", code: "CUSTOMER_NOT_FOUND", message: "Customer not found" }]]></ee:set-payload>
                        </ee:message>
                    </ee:transform>
                </when>
                <otherwise>
                    <set-variable variableName="httpStatus" value="200" />
                    <ee:transform>
                        <ee:message>
                            <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ customerId: payload[0].CUSTOMER_ID, name: payload[0].NAME, email: payload[0].EMAIL, mobileNumber: payload[0].MOBILE_NUMBER, status: "SUCCESS" }]]></ee:set-payload>
                        </ee:message>
                    </ee:transform>
                </otherwise>
            </choice>

            <error-handler>
                <on-error-continue type="ANY" logException="true">
                    <set-variable variableName="httpStatus" value="500" />
                    <ee:transform>
                        <ee:message>
                            <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{ status: "FAILED", code: "CUSTOMER_READ_FAILED", message: "Customer could not be retrieved" }]]></ee:set-payload>
                        </ee:message>
                    </ee:transform>
                </on-error-continue>
            </error-handler>
        </try>
    </flow>

</mule>
